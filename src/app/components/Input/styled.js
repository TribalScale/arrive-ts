import styled from '@emotion/native';
import CaptionText from '../Text/Caption';

export const InputContainer = styled.View({
  width: '100%',
  marginBottom: 4,
});

export const LabelText = styled(CaptionText)`
  margin-bottom: 4px;
`;
